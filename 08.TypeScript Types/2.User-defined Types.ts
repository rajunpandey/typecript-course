/*
//1.Array
//The array data type is a collection of the same elements.
// It stores the elements, which you can access or update using the array index that starts from 0.
//example
let numbers: number[] = [1, 2, 3];
*/

/*
//2.Tuple
//Tuple types allow you to express an array with a fixed number of elements whose types are known, but need not be the same
//example
let tuple: [string, number] = ["hello", 10];
console.log(tuple);
*/

//3.Enum
//An enumeration is a collection of related values that can be numeric or string values.
//example
enum Color {Red, Green, Blue}
let c: Color = Color.Green;
console.log(c);