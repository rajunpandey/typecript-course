//This is a combination of both numeric and string enums. 
//That is, in this type of enum, we can assign both string values or numeric values to its members.

enum student{
    Name = "rajun",
    Gender = "male",
    Mobile = 9811360746,
  }
  console.log(student.Name);
  console.log(student.Gender);
  console.log(student.Mobile);