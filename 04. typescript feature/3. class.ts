//Classes are a blueprint of the objects. 
//Classes can contain properties, and methods which can be accessed using an instance of classes. 
/*
// Basic example of class
class Greeter {
    greeting: string;
    // Constructor method
    constructor(message: string) {
      this.greeting = message;
    }
    // Class Method
    greet() {
      return "Hello, " + this.greeting;
    }
  }
  
  // Create an instance of the class
  let greeter = new Greeter("world");
  console.log(greeter.greet()); // Hello, world
*/
